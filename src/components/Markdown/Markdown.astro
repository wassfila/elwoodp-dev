<article>
  <slot />
</article>

<style>
  @import '../../styles/mixins/mixins.css';

  article {
    display: grid;
    @mixin fluid gap, 2, 2.5;
    @mixin fluid font-size, 1, 1.125;
  }

  article :global(a) {
    color: var(--blue);
    &:hover {
      text-decoration: underline;
    }
  }

  article :global(blockquote) {
    @mixin fluid padding, 1, 1.5;
    @mixin theme-old background-color, var(--gray-925), var(--gray-100);
    white-space: normal;
    border-radius: 8px;
    border-left: 8px solid;
    @mixin theme-old border-color, var(--gray-700), var(--gray-200);
  }

  /* Code Block (Prism) */

  article :global(pre) {
    @mixin fluid padding, 1, 1.5;
    @mixin theme-old background-color, var(--gray-925) !important, var(--gray-900) !important;
    white-space: normal;
    border-radius: 8px;
  }

  article :global(code) {
    @mixin fluid font-size, 0.875, 1;
  }

  /* Headings  */
  
  article :global(h1) {
    display: none;
  }

  article :global(h2),
  article :global(h3),
  article :global(h4),
  article :global(h5),
  article :global(h6) {
    margin-top: 0.5em;
    margin-bottom: -0.25em;
    @mixin theme-old color, var(--gray-50), var(--gray-800);
    @mixin theme-old font-weight, 700, 775;
  }

  article :global(h2) {
    @mixin fluid font-size, 1.75, 2.25;
    /* @mixin fluid font-size, 1.5, 2; */
    line-height: 130%;
  }

  article :global(h3) {
    @mixin fluid font-size, 1.5, 2;
    line-height: 130%;
  }

  /* Lists  */

  article :global(ul) {
    list-style: disc;
    padding-left: 1.25rem;
  }
  article :global(li) {
    list-style-position: outside;
  }

  article :global(li input) {
    display: none; /* TODO: Needs styling rather than hiding - Astro doesn't add anything helpful to markup for targetting */
  }
</style>
