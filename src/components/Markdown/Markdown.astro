<article>
  <slot />
</article>

<style>
  @import '../../styles/vars.css';
  @import '../../styles/mixins/mixins.css';

  article {
    display: grid;
    row-gap: fluid(24px, 24px, toRem);
    font-size: fluid(16px, 18px, toRem);
    line-height: 155%;
  }

  article :global(a) {
    color: $blue;
    &:hover {
      color: $blue-500;
    }
  }

  article :global(blockquote) {
    position: relative;
    padding: fluid(16px, 24px, toRem);
    padding-left: 76px;
    @mixin theme(background-color, alpha($blue, 6%), alpha($blue, 14%));
    white-space: normal;
    border-radius: 8px;
    &:after,
    &:before {
      content: '';
      position: absolute;
      top: calc(50% - 17px);
      right: 0;
      bottom: 0;
      left: 21px;
      width: 28px;
      height: 28px;
      border: 3px solid;
      box-sizing: content-box;
    }
    &:after {
      @mixin theme(background-color, $gray-50, $gray-850);
      mask-image: url('../../assets/icons/icon-info.svg');
      mask-repeat: no-repeat;
      mask-position: center;
    }
    &:before {
      border-radius: 100px;
      background-color: $blue;
      background-clip: padding-box;
      @mixin theme(border-color, alpha($blue, 35%), alpha($blue, 65%));
    }
  }

  /* Code Block (shiki + remark-code-extra - see astro.config) */
  article :global(pre) {
    background: transparent !important;
    white-space: pre-wrap;
  }

  :global(div.codeblock) {
    padding-block: fluid(24px, 24px, toRem);
    padding-inline: fluid(24px, 36px, toRem);
    @mixin theme(background-color, $gray-950, $gray-975);
    border-radius: 8px;
    font-size: toRem(14px);
    line-height: 170%;
    position: relative;
    margin: toRem(20px) -36px;
    white-space: normal;
    font-family: Menlo, Monaco, 'Andale Mono', 'Ubuntu Mono', monospace;
  }

  /* language badge */
  :global(div.codeblock:before) {
    position: absolute;
    right: 16px;
    top: 16px;
    color: rgb(119 120 123);
    font-size: toRem(12px);
    line-height: 100%;
  }

  :global(div.codeblock--js:before, div.codeblock--javascript:before) {
    content: 'js';
  }

  :global(div.codeblock--html:before) {
    content: 'html';
  }

  :global(div.codeblock--css:before) {
    content: 'css';
  }

  :global(div.codeblock--shell:before, div.codeblock--bash:before, div.codeblock--sh:before) {
    content: 'sh';
  }

  /* Headings */

  article :global(h1) {
    display: none;
  }

  article :global(h2),
  article :global(h3),
  article :global(h4),
  article :global(h5),
  article :global(h6) {
    @mixin theme-old color, var(--gray-50), var(--gray-800);
    font-weight: 600;
  }

  article :global(h2) {
    margin-top: fluid(32px, 32px, toRem);
    margin-bottom: fluid(8px, 8px, toRem);
    font-size: fluid(36px, 36px, toRem);
    line-height: 130%;
  }

  article :global(h3) {
    font-size: fluid(30, 30, toRem);
    line-height: 130%;
  }

  /* Lists  */

  article :global(ul) {
    list-style: disc;
    padding-left: 1.25rem;
  }
  article :global(li) {
    list-style-position: outside;
  }

  article :global(li input) {
    display: none; /* TODO: Needs styling rather than hiding - Astro doesn't add anything helpful to markup for targetting */
  }
</style>
